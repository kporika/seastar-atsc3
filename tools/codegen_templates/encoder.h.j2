// SPDX-License-Identifier: Apache-2.0
// AUTOGENERATED by tools/codegen.py — DO NOT EDIT.
//
// Encoder for protocol: {{ p.name }} ({{ p.spec }}).
// Inverse of {{ p.name }}_decoder.h: builds a wire-format byte sequence
// from a populated decoded_t.

#pragma once

#include <cstddef>
#include <string>
#include <vector>

#include "{{ p.name }}_decoder.h"

namespace atsc3::{{ p.name }} {

struct encode_result {
    bool ok = false;
    std::vector<std::byte> bytes;
    std::string error;
};

// Encode `d` to wire format. Always allocates a fresh buffer; caller
// transfers ownership via move.
encode_result encode(const decoded_t &d);

}  // namespace atsc3::{{ p.name }}
